N: 5
UPTO: true
column_info: "original"
use_ppath: false
data_dir: "./Data"

# List of shapes to use – the strings must match keys in SHAPES_MAP.
shapes:
#  - "3col_less"
  # - "2row_less"
  # - "3row_less"
   - "all"
  # - "hook"

# List of filters to use – the strings must match keys in FILTERS_MAP.
filters:
#  - "with_inductive_connectedness_criterion"
   - "with_all_row_connectedness_criterion"

# List of connectedness options – the strings must match keys in CONNECTED_MAP.
connectedness:
  - "connected"
    #- "disconnected"
